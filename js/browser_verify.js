function browser_verify(){
    // var isFirefox = navigator.userAgent;
    var isFirefox = navigator.userAgent.toLowerCase().indexOf('firefox') > -1;
    if(isFirefox){
        
    }
    console.log(isFirefox);
}